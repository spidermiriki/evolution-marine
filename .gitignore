# ── Dossiers hors dépôt ───────────────────────────────────────────────────────
index.html
évolution-marine-(4.6)/

# ── Cache de l'éditeur Godot 4 (généré automatiquement, ne pas versionner) ───
godot/.godot/

# ── Fichiers d'import Godot (générés automatiquement) ────────────────────────
*.import

# ── Assets binaires lourds (sons, musiques) ───────────────────────────────────
*.ogg
*.mp3
*.wav

# ── Config d'export (peut contenir des chemins locaux) ───────────────────────
export_presets.cfg

# ── Traductions compilées (générées depuis les CSV) ──────────────────────────
*.translation

# ── Fichiers système ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
